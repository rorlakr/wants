<%= simple_form_for [commentable, commentable.comments.build], remote: true, authenticity_token: true do | f | %>
  <%= f.input :content, label: false, placeholder: '댓글을 달아 주세요. 엔터키를 누르면 저장됩니다.', input_html: { class: 'comment_text', rows: 5} %>
  <%#= f.submit class:'btn btn-default btn-xs' %>
<% end %>